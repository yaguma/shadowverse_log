# TDD要件定義: Statistics Dashboard実装 - 基本統計

**タスクID**: TASK-0018
**タスク名**: Statistics Dashboard実装 - 基本統計
**実装タイプ**: TDD Process
**作成日**: 2025-11-08
**ステータス**: 要件定義完了

---

## 1. 機能の概要（EARS要件定義書・設計文書ベース）

### 🔵 何をする機能か

Statistics Dashboard（統計ダッシュボード）は、ユーザーが登録した対戦履歴から統計情報を集計・表示するReact Pageコンポーネントです。以下の主要機能を提供します：

1. **StatisticsDashboardPage** - ページコンテナ（ヘッダー、期間選択、統計表示）
2. **PeriodSelector** - 集計期間選択フォーム（開始日、終了日）
3. **OverallStats** - 全体統計表示（総試合数、勝数、敗数、勝率）
4. **DeckStatsTable** - デッキ別統計テーブル（マイデッキ・相手デッキ）
5. **RankStatsTable** - ランク帯別統計テーブル
6. **TurnStats** - 先攻後攻別統計表示
7. **Loading/Error States** - ローディング・エラー表示
8. **Responsive Design** - モバイル対応レイアウト

### 🔵 どのような問題を解決するか

- **REQ-201**: 独立した統計・分析ダッシュボード画面を提供（ユーザーが対戦成績を分析できる）
- **REQ-202**: 集計期間を選択可能な機能を提供（特定期間の成績を確認できる）
- **REQ-203**: 統計情報を表示（全体勝率、デッキ別勝率、相手デッキ別勝率、ランク帯別成績）
- **REQ-405**: 集計期間にデータが存在しない場合、「データなし」のメッセージを表示
- **REQ-502**: データ読み込み中はローディングインジケーターを表示

### 🔵 想定されるユーザー

- **個人ユーザー**: シャドウバースプレイヤー（Phase 1は単一ユーザー）
- **使用シーン**: 対戦履歴を分析し、デッキや戦略の改善点を見つける
- **操作頻度**: 中〜高頻度（週に数回〜毎日）

### 🔵 システム内での位置づけ

- **階層**: プレゼンテーション層（Frontend - React Page Component）
- **ルーティング**: `/statistics` （React Router v7）
- **依存関係**:
  - **TASK-0010**: Backend API（`GET /api/statistics`）✅ 完了
  - **TASK-0015**: Zustand State Management（将来的に統計用Storeを追加する可能性）✅ 完了
  - **共有型定義**: `frontend/src/types/index.ts` (`StatisticsResponse`)

### 参照した要件・設計文書

- **参照したEARS要件**: REQ-201, REQ-202, REQ-203, REQ-405, REQ-502
- **参照した設計文書**:
  - `docs/spec/shadowverse-battle-log-requirements.md` - 統計・分析ダッシュボード機能要件
  - `docs/implements/shadowverse-battle-log/TASK-0010/statistics-api-requirements.md` - Backend API仕様
  - `frontend/src/types/index.ts` - StatisticsResponse型定義

---

## 2. 入力・出力の仕様（EARS機能要件・TypeScript型定義ベース）

### 🔵 入力パラメータ

#### StatisticsDashboardPageコンポーネント

**プロップス**: なし（ルーティングされたページコンポーネント）

**内部State（useState）**:

```typescript
const [startDate, setStartDate] = useState<string>(''); // 集計開始日 (YYYY-MM-DD)
const [endDate, setEndDate] = useState<string>('');     // 集計終了日 (YYYY-MM-DD)
const [statistics, setStatistics] = useState<StatisticsResponse | null>(null);
const [isLoading, setIsLoading] = useState<boolean>(false);
const [error, setError] = useState<string | null>(null);
```

**期間選択のデフォルト値**:
- **startDate**: 今日の7日前（`new Date()` から7日前を計算）🟡
- **endDate**: 今日の日付（`new Date()` をYYYY-MM-DD形式に変換）🟡

#### PeriodSelectorコンポーネント

```typescript
interface PeriodSelectorProps {
  /** 開始日（YYYY-MM-DD形式） */
  startDate: string;
  /** 終了日（YYYY-MM-DD形式） */
  endDate: string;
  /** 開始日変更時のコールバック関数 */
  onStartDateChange: (date: string) => void;
  /** 終了日変更時のコールバック関数 */
  onEndDateChange: (date: string) => void;
  /** 検索実行時のコールバック関数 */
  onSearch: () => void;
  /** ローディング状態 */
  isLoading: boolean;
}
```

#### OverallStatsコンポーネント

```typescript
interface OverallStatsProps {
  /** 全体統計データ */
  stats: {
    totalGames: number;
    wins: number;
    losses: number;
    winRate: number; // 小数点第1位まで (例: 66.7)
  };
}
```

#### DeckStatsTableコンポーネント

```typescript
interface DeckStatsTableProps {
  /** タイトル（"マイデッキ別統計" or "相手デッキ別統計"） */
  title: string;
  /** デッキ別統計データ */
  deckStats: Array<{
    deckId: string;
    deckName: string;
    totalGames: number;
    wins: number;
    losses: number;
    winRate: number;
  }>;
}
```

#### RankStatsTableコンポーネント

```typescript
interface RankStatsTableProps {
  /** ランク帯別統計データ */
  rankStats: Array<{
    rank: Rank;
    group: Group;
    totalGames: number;
    wins: number;
    losses: number;
    winRate: number;
  }>;
}
```

#### TurnStatsコンポーネント

```typescript
interface TurnStatsProps {
  /** 先攻後攻別統計データ */
  turnStats: {
    先攻: {
      totalGames: number;
      wins: number;
      losses: number;
      winRate: number;
    };
    後攻: {
      totalGames: number;
      wins: number;
      losses: number;
      winRate: number;
    };
  };
}
```

### 🔵 出力値

#### API リクエスト

```typescript
// GET /api/statistics?startDate=2025-01-01&endDate=2025-01-31
// クエリパラメータ:
// - startDate: string (YYYY-MM-DD形式, optional, default: endDateの7日前)
// - endDate: string (YYYY-MM-DD形式, optional, default: 今日)
```

#### API レスポンス（成功時）

```typescript
{
  success: true,
  data: {
    overall: {
      totalGames: 150,
      wins: 98,
      losses: 52,
      winRate: 65.3
    },
    byMyDeck: [
      {
        deckId: "deck_001",
        deckName: "秘術オデンスペル",
        totalGames: 75,
        wins: 50,
        losses: 25,
        winRate: 66.7
      },
      // ...more decks
    ],
    byOpponentDeck: [
      {
        deckId: "deck_master_005",
        deckName: "進化ネクロ",
        totalGames: 30,
        wins: 20,
        losses: 10,
        winRate: 66.7
      },
      // ...more decks
    ],
    byRank: [
      {
        rank: "ダイアモンド",
        group: "AAA",
        totalGames: 80,
        wins: 55,
        losses: 25,
        winRate: 68.8
      },
      // ...more ranks
    ],
    byTurn: {
      先攻: {
        totalGames: 78,
        wins: 52,
        losses: 26,
        winRate: 66.7
      },
      後攻: {
        totalGames: 72,
        wins: 46,
        losses: 26,
        winRate: 63.9
      }
    },
    dateRange: {
      startDate: "2025-01-01",
      endDate: "2025-01-31"
    }
  },
  meta: {
    timestamp: "2025-01-31T12:34:56.789Z",
    requestId: "invocation-id-12345"
  }
}
```

#### データなしの場合 (200 OK)

```typescript
{
  success: true,
  data: {
    overall: {
      totalGames: 0,
      wins: 0,
      losses: 0,
      winRate: 0
    },
    byMyDeck: [],
    byOpponentDeck: [],
    byRank: [],
    byTurn: {
      先攻: { totalGames: 0, wins: 0, losses: 0, winRate: 0 },
      後攻: { totalGames: 0, wins: 0, losses: 0, winRate: 0 }
    },
    dateRange: {
      startDate: "2025-01-01",
      endDate: "2025-01-07"
    }
  },
  // ...meta
}
```

### 🔵 データフロー（正常系）

```
1. ページ初期表示
   - デフォルト期間（7日間）を設定
   - API呼び出し（GET /api/statistics?startDate=...&endDate=...）
   - ローディング表示

2. API レスポンス受信
   - statistics stateに保存
   - OverallStats, DeckStatsTable, RankStatsTable, TurnStatsを表示

3. 期間変更
   - ユーザーがstartDate/endDateを変更
   - 検索ボタンクリック
   - API再呼び出し
   - ローディング表示 → 統計更新
```

### 🔵 データフロー（エラー系）

```
1. ネットワークエラー
   - API呼び出し失敗
   - error stateに「ネットワークエラーが発生しました」を設定
   - エラーメッセージ表示

2. サーバーエラー (500)
   - API レスポンス: { success: false, error: { message: "..." } }
   - error stateにエラーメッセージを設定
   - エラーメッセージ表示
```

---

## 3. 制約条件（EARS非機能要件・アーキテクチャ設計ベース）

### 🔵 パフォーマンス要件

- **初期表示速度**: 3秒以内に統計情報を表示 🟡（REQ-XXX パフォーマンス要件から推測）
- **API レスポンスタイム**: Backend APIは500ms以内にレスポンスを返す（TASK-0010で保証済み）🔵
- **再検索速度**: 期間変更後1秒以内に統計情報を更新 🟡

### 🔵 セキュリティ要件

- **Phase 1**: 認証なし（単一ユーザー想定）🔵
- **Phase 2**: Azure AD B2C認証が必要（REQ-701）🔵
- **入力バリデーション**: 日付形式チェック（YYYY-MM-DD）、startDate <= endDate 🟡

### 🔵 互換性要件

- **REQ-603**: PC・スマートフォンの両方に対応したレスポンシブデザイン 🔵
- **ブラウザ対応**: Chrome, Safari, Firefox, Edge（最新2バージョン）🟡
- **モバイルビューポート**: 320px以上 🟡

### 🔵 アーキテクチャ制約

- **Reactコンポーネント設計**: 関心の分離（プレゼンテーション層とロジック層）🔵
- **状態管理**: useState（将来的にZustand Storeに移行する可能性あり）🟡
- **API呼び出し**: apiClient（`frontend/src/api/client.ts`）を使用 🔵
- **スタイリング**: Tailwind CSS 🔵

### 🔵 API制約

- **エンドポイント**: `GET /api/statistics` 🔵（TASK-0010で実装済み）
- **クエリパラメータ**:
  - `startDate`: YYYY-MM-DD形式（optional）
  - `endDate`: YYYY-MM-DD形式（optional）
- **レスポンス形式**: ApiResponse<StatisticsResponse> 🔵

### 参照した要件・設計文書

- **参照したEARS要件**: REQ-603 (レスポンシブデザイン), REQ-701 (Phase 2認証)
- **参照した設計文書**:
  - `docs/tech-stack.md` - フロントエンド技術スタック（React, TypeScript, Tailwind CSS, Zustand）
  - `docs/implements/shadowverse-battle-log/TASK-0010/statistics-api-requirements.md` - API仕様

---

## 4. 想定される使用例（EARSエッジケース・データフローベース）

### 🔵 基本的な使用パターン（通常要件REQ-XXXから抽出）

#### UC-001: ページ初期表示

```
Given: ユーザーが統計ダッシュボードページにアクセス
When: ページが読み込まれる
Then:
  - デフォルト期間（過去7日間）が設定される
  - ローディングスピナーが表示される
  - API呼び出しが実行される
  - 統計情報が表示される
```

🔵 信頼性レベル: REQ-201, REQ-202より

#### UC-002: 期間変更検索

```
Given: 統計ダッシュボードが表示されている
When: ユーザーがstartDate="2025-01-01", endDate="2025-01-31"を選択し、検索ボタンをクリック
Then:
  - ローディングスピナーが表示される
  - API呼び出しが実行される（GET /api/statistics?startDate=2025-01-01&endDate=2025-01-31）
  - 2025年1月の統計情報が表示される
```

🔵 信頼性レベル: REQ-202より

#### UC-003: 全体統計の確認

```
Given: 統計データが表示されている
When: ユーザーが全体統計セクションを確認
Then:
  - 総試合数、勝数、敗数、勝率が表示される
  - 勝率は小数点第1位まで表示される（例: 66.7%）
```

🔵 信頼性レベル: REQ-203より

#### UC-004: デッキ別統計の確認

```
Given: 統計データが表示されている
When: ユーザーがデッキ別統計テーブルを確認
Then:
  - マイデッキ別統計が表示される（デッキ名、試合数、勝数、敗数、勝率）
  - 相手デッキ別統計が表示される（デッキ名、試合数、勝数、敗数、勝率）
  - ソート順: 試合数が多い順
```

🔵 信頼性レベル: REQ-203より

### 🟡 エッジケース（EDGE-XXXから抽出）

#### EDGE-001: データが0件の場合

```
Given: 選択した期間に対戦履歴が存在しない
When: 統計APIを呼び出す
Then:
  - totalGames: 0, wins: 0, losses: 0, winRate: 0 が表示される
  - 「指定期間にデータがありません」のメッセージが表示される
```

🔵 信頼性レベル: REQ-405より

#### EDGE-002: 開始日 > 終了日の場合

```
Given: ユーザーがstartDate="2025-01-31", endDate="2025-01-01"を選択
When: 検索ボタンをクリック
Then:
  - バリデーションエラーメッセージが表示される
  - 「開始日は終了日以前の日付を選択してください」
  - API呼び出しは実行されない
```

🟡 信頼性レベル: 一般的なUX要件から推測

#### EDGE-003: 未来の日付を選択した場合

```
Given: ユーザーがendDate="2099-12-31"を選択
When: 検索ボタンをクリック
Then:
  - バリデーションエラーメッセージが表示される
  - 「未来の日付は選択できません」
  - API呼び出しは実行されない
```

🟡 信頼性レベル: REQ-401（対戦履歴登録の未来日付禁止）から類推

### 🔵 エラーケース（EDGE-XXXエラー処理から抽出）

#### ERROR-001: ネットワークエラー

```
Given: 統計ダッシュボードが表示されている
When: ネットワーク接続が切れている状態でAPI呼び出しが実行される
Then:
  - エラーメッセージが表示される
  - 「ネットワークエラーが発生しました。接続を確認してください。」
  - 再試行ボタンが表示される
```

🔵 信頼性レベル: 一般的なエラーハンドリング要件

#### ERROR-002: サーバーエラー (500)

```
Given: 統計ダッシュボードが表示されている
When: Backend APIがサーバーエラー（500）を返す
Then:
  - エラーメッセージが表示される
  - 「サーバーエラーが発生しました。しばらくしてから再試行してください。」
  - 再試行ボタンが表示される
```

🔵 信頼性レベル: 一般的なエラーハンドリング要件

### 参照した要件・設計文書

- **参照したEARS要件**: REQ-201, REQ-202, REQ-203, REQ-405, REQ-401
- **参照した設計文書**:
  - `docs/implements/shadowverse-battle-log/TASK-0010/statistics-api-requirements.md` - API仕様とエッジケース

---

## 5. EARS要件・設計文書との対応関係

### 参照したユーザストーリー

- **US-003**: プレイヤーとして、期間を指定して統計情報を確認したい 🔵

### 参照した機能要件

- **REQ-201**: システムは独立した統計・分析ダッシュボード画面を提供しなければならない 🔵
- **REQ-202**: システムは集計期間を選択可能な機能を提供しなければならない 🔵
- **REQ-203**: システムは統計情報を表示しなければならない（全体勝率、デッキ別勝率、相手デッキ別勝率、ランク帯別成績）🔵
- **REQ-405**: 集計期間にデータが存在しない場合、システムは「データなし」のメッセージを表示しなければならない 🔵
- **REQ-502**: データ読み込み中の状態である場合、システムはローディングインジケーターを表示しなければならない 🔵
- **REQ-603**: システムはPC・スマートフォンの両方に対応したレスポンシブデザインでなければならない 🔵

### 参照した非機能要件

- **NFR-101**: ページロード時間が3秒以内 🔵

### 参照したエッジケース

- **EDGE-001**: データが0件の場合 🔵
- **EDGE-002**: 開始日 > 終了日の場合 🟡（推測）
- **EDGE-003**: 未来の日付を選択した場合 🟡（REQ-401から類推）
- **ERROR-001**: ネットワークエラー 🔵
- **ERROR-002**: サーバーエラー (500) 🔵

### 参照した設計文書

- **アーキテクチャ**: `docs/tech-stack.md` - フロントエンド技術スタック
- **データフロー**: `docs/implements/shadowverse-battle-log/TASK-0010/statistics-api-requirements.md` - 統計API仕様
- **型定義**: `frontend/src/types/index.ts` - StatisticsResponse, Rank, Group
- **API仕様**: `docs/implements/shadowverse-battle-log/TASK-0010/statistics-api-requirements.md` - GET /api/statistics

---

## 6. コンポーネント設計（補足）

### 🔵 コンポーネント階層構造

```
StatisticsDashboardPage/
├── PeriodSelector              # 期間選択フォーム
│   ├── Input (startDate)
│   ├── Input (endDate)
│   └── Button (検索)
├── Loading                     # ローディングスピナー
├── Error                       # エラーメッセージ
├── EmptyState                  # データなしメッセージ
└── StatisticsContent           # 統計情報表示エリア
    ├── OverallStats            # 全体統計
    ├── DeckStatsTable          # マイデッキ別統計
    ├── DeckStatsTable          # 相手デッキ別統計
    ├── RankStatsTable          # ランク帯別統計
    └── TurnStats               # 先攻後攻別統計
```

### 🟡 レスポンシブデザイン

- **Desktop (>= 768px)**: テーブル表示、2カラムレイアウト
- **Mobile (< 768px)**: カード表示、1カラムレイアウト

---

## 品質判定

✅ **高品質**:
- ✅ 要件の曖昧さ: なし（EARS要件定義書から明確に抽出）
- ✅ 入出力定義: 完全（TypeScript型定義が存在、API仕様が明確）
- ✅ 制約条件: 明確（技術スタック、アーキテクチャ制約、API制約が定義済み）
- ✅ 実装可能性: 確実（TASK-0010 Statistics APIが実装済み、型定義が存在）

**判定結果**: ✅ 高品質 - TDDテストケース作成フェーズへ進む準備が整いました

---

## 次のステップ

次のお勧めステップ: `/tsumiki:tdd-testcases` でテストケースの洗い出しを行います。
