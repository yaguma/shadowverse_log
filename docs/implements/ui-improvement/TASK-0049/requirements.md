# TASK-0049: BattleLogForm本実装（モック→API連携）

## 要件定義書

### 概要

BattleLogFormコンポーネントの仮実装（モックデータ）を、API連携による本実装に変更する。

### 信頼性レベル

🔵 **青信号**: 要件定義書・現在のBattleLogForm.tsxのコメント（仮実装指示）に基づく

### 背景・目的

現在のBattleLogFormコンポーネントでは、マイデッキ一覧とデッキマスター一覧がモックデータとして直接コード内にハードコードされている。これを、TASK-0048で実装したdeckStoreを使用してAPIからデータを取得する本実装に変更する。

### 現在の仮実装（削除対象）

```typescript
// 【モックデータ】: テストを通すための仮データ（空配列の場合を考慮） 🟡
const mockDecks = [
  { id: 'deck-001', deckName: 'テストデッキ1' },
  { id: 'deck-002', deckName: 'テストデッキ2' },
];
setMyDecks(mockDecks);
```

```typescript
// 【モックデータ】: テストを通すための仮データ（空配列の場合を考慮） 🟡
const mockDeckMasters = [
  { id: 'deck-master-001', deckName: '相手デッキ1' },
  { id: 'deck-master-002', deckName: '相手デッキ2' },
  { id: 'deck-master-005', deckName: '相手デッキ5' },
];
setDeckMasters(mockDeckMasters);
```

### 機能要件

#### REQ-0049-001: デッキマスター一覧のAPI連携

- **条件**: BattleLogFormコンポーネントがマウントされた時
- **動作**: useDeckStoreのfetchDeckMastersを呼び出してデッキマスター一覧を取得する
- **結果**: 取得したデッキマスター一覧が「相手デッキ」ドロップダウンに表示される

#### REQ-0049-002: マイデッキ一覧の暫定対応

- **条件**: BattleLogFormコンポーネントがマウントされた時
- **動作**: マイデッキ一覧はAPIが未実装のため、暫定的にモックデータを維持する
- **結果**: マイデッキ一覧が「使用デッキ」ドロップダウンに表示される
- **備考**: マイデッキAPI実装後に本実装に変更予定

#### REQ-0049-003: ローディング状態の表示

- **条件**: デッキマスター取得APIが実行中の場合
- **動作**: フォーム全体のローディング状態を表示する
- **結果**: ローディング中はスピナーまたは適切な表示がされる

#### REQ-0049-004: エラー状態の表示

- **条件**: デッキマスター取得APIがエラーを返した場合
- **動作**: エラーメッセージを表示する
- **結果**: 「デッキ情報の取得に失敗しました」等のエラーメッセージが表示される

### 非機能要件

- TypeScriptコンパイルエラーがないこと
- 既存のテストが通ること（テストの修正が必要な場合は適切に修正する）
- ローディング状態が適切に管理されること

### 変更対象ファイル

1. `frontend/src/components/battle-log/BattleLogForm.tsx`
   - useDeckStoreのインポート追加
   - モックデータの削除
   - fetchDeckMastersの呼び出し追加
   - ローディング状態の統合

2. `frontend/src/components/battle-log/BattleLogForm.test.tsx`
   - useDeckStoreのモック追加
   - テストケースの修正（API連携対応）

### 受け入れ基準

- [ ] デッキマスター一覧がAPIから取得される
- [ ] マイデッキ一覧が表示される（暫定モック）
- [ ] モックデータ（デッキマスター）が削除されている
- [ ] ローディング中は適切な表示がされる
- [ ] エラー時は適切なメッセージが表示される
- [ ] TypeScriptコンパイルエラーがない
- [ ] 既存テストが通る（または適切に修正される）

### 技術的実装方針

```typescript
import { useDeckStore } from '../../store/deckStore';

const { deckMasters, fetchDeckMasters, isLoading: isDeckLoading, error: deckError } = useDeckStore();

useEffect(() => {
  fetchDeckMasters();
}, [fetchDeckMasters]);
```

### 依存関係

- TASK-0047: 型定義の拡張（完了済み）
- TASK-0048: deckStoreへのAPI連携実装（完了済み）
