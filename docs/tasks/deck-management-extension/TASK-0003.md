# TASK-0003: バリデーション共通ユーティリティ実装

**タスクID**: TASK-0003
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 1 - 基盤・共通コンポーネント
**信頼性レベル**: 🔵 *requirements.mdのバリデーション要件より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/deck-management-extension/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/deck-management-extension/architecture.md)
- **インターフェース定義**: [📐 interfaces.ts](../../design/deck-management-extension/interfaces.ts)

## タスク概要

デッキ管理機能で使用するバリデーションの共通ユーティリティを実装する。Zodスキーマを使用し、deckName、className、deckCodeのバリデーションルールを実装する。既存のバリデーションスキーマを拡張し、要件定義に沿ったバリデーションを行う。

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md) - 共通インターフェース・型定義の実装
- **後続タスク**: TASK-0006, TASK-0007, TASK-0015（Phase 2以降のタスク）

## 完了条件

- [ ] DeckMasterCreateRequestのZodスキーマが実装されている
- [ ] DeckMasterUpdateRequestのZodスキーマが実装されている
- [ ] MyDeckCreateRequestのZodスキーマが実装されている（deckCodeは空白可）
- [ ] deckNameは必須・1-100文字のバリデーションが実装されている
- [ ] classNameはCLASS_NAMESに含まれる値のみ許可されている
- [ ] deckCodeはバリデーションなし（空白可）が確認されている
- [ ] 全テストケースがパスしている
- [ ] TypeScriptコンパイルエラーがないこと

---

## 実装詳細

### 1. DeckMasterCreateRequest Zodスキーマ 🔵

**信頼性**: 🔵 *requirements.md REQ-EXT-001〜REQ-EXT-005対応*

デッキ種別追加時のバリデーションスキーマを実装する。

```typescript
import { z } from 'zod';
import { CLASS_NAMES } from '../constants/index.js';

/**
 * クラス名スキーマ（CLASS_NAMES定数から生成）
 */
export const ClassNameSchema = z.enum(CLASS_NAMES, {
  errorMap: () => ({ message: '無効なクラス名です' }),
});

/**
 * デッキ名スキーマ（必須、1-100文字）
 */
export const DeckNameSchema = z
  .string()
  .min(1, 'デッキ名は必須です')
  .max(100, 'デッキ名は100文字以内で入力してください');

/**
 * DeckMasterCreateRequest バリデーションスキーマ
 * REQ-EXT-001〜REQ-EXT-005 対応
 */
export const DeckMasterCreateRequestSchema = z.object({
  className: ClassNameSchema,
  deckName: DeckNameSchema,
});

export type DeckMasterCreateRequestInput = z.infer<typeof DeckMasterCreateRequestSchema>;
```

**実装ファイル**: `packages/shared/src/validation/deck-master.ts`（拡張）

---

### 2. DeckMasterUpdateRequest Zodスキーマ 🔵

**信頼性**: 🔵 *requirements.md REQ-EXT-006〜REQ-EXT-007対応*

デッキ種別更新時のバリデーションスキーマを実装する。classNameは変更不可のため含まない。

```typescript
/**
 * DeckMasterUpdateRequest バリデーションスキーマ
 * REQ-EXT-006〜REQ-EXT-007 対応
 * classNameは変更不可のため含まない
 */
export const DeckMasterUpdateRequestSchema = z.object({
  deckName: DeckNameSchema,
});

export type DeckMasterUpdateRequestInput = z.infer<typeof DeckMasterUpdateRequestSchema>;
```

**実装ファイル**: `packages/shared/src/validation/deck-master.ts`（拡張）

---

### 3. MyDeckCreateRequest Zodスキーマ 🔵

**信頼性**: 🔵 *requirements.md REQ-EXT-101〜REQ-EXT-107対応*

使用デッキ追加時のバリデーションスキーマを実装する。deckCodeはバリデーションなし（空白可）。

```typescript
/**
 * デッキコードスキーマ（任意、バリデーションなし）
 * REQ-EXT-107: deckCodeにはバリデーションを適用しない
 */
export const DeckCodeOptionalSchema = z.string().optional();

/**
 * デッキIDスキーマ（UUID形式）
 */
export const DeckIdSchema = z.string().uuid('無効なデッキIDです');

/**
 * MyDeckCreateRequest バリデーションスキーマ
 * REQ-EXT-101〜REQ-EXT-107 対応
 */
export const MyDeckCreateRequestSchema = z.object({
  deckId: DeckIdSchema,
  deckName: DeckNameSchema,
  deckCode: DeckCodeOptionalSchema,
});

export type MyDeckCreateRequestInput = z.infer<typeof MyDeckCreateRequestSchema>;
```

**実装ファイル**: `packages/shared/src/validation/my-deck.ts`（拡張）

---

### 4. バリデーション実行ユーティリティ 🔵

**信頼性**: 🔵 *既存パターンに従う*

Zodスキーマを使用したバリデーション実行ユーティリティを実装する。

```typescript
import { ZodSchema, ZodError } from 'zod';
import type { ValidationError } from '../types/api.js';

/**
 * Zodバリデーションエラーを ValidationError 形式に変換する
 */
export function zodErrorToValidationError(zodError: ZodError): ValidationError {
  return {
    code: 'VALIDATION_ERROR',
    message: 'バリデーションエラー',
    details: zodError.errors.map((err) => ({
      field: err.path.join('.'),
      constraint: err.code,
      value: undefined,
    })),
  };
}

/**
 * データをバリデーションし、エラーがあればValidationErrorを返す
 */
export function validate<T>(schema: ZodSchema<T>, data: unknown):
  { success: true; data: T } | { success: false; error: ValidationError } {
  const result = schema.safeParse(data);
  if (result.success) {
    return { success: true, data: result.data };
  }
  return { success: false, error: zodErrorToValidationError(result.error) };
}
```

**実装ファイル**: `packages/shared/src/validation/utils.ts`（新規作成）

---

## 単体テスト要件

### DeckMasterCreateRequestSchema - 正常系 🔵

**信頼性**: 🔵 *スキーマバリデーションテスト*

**Given**: 有効なclassName（"エルフ"）とdeckName（"テストデッキ"）が与えられる
**When**: DeckMasterCreateRequestSchemaでバリデーションする
**Then**: バリデーションが成功する

---

### DeckMasterCreateRequestSchema - deckName空文字 🔵

**信頼性**: 🔵 *REQ-EXT-403対応*

**Given**: 空文字のdeckNameが与えられる
**When**: DeckMasterCreateRequestSchemaでバリデーションする
**Then**: "デッキ名は必須です"エラーが返される

---

### DeckMasterCreateRequestSchema - deckName101文字 🔵

**信頼性**: 🔵 *文字数制限テスト*

**Given**: 101文字のdeckNameが与えられる
**When**: DeckMasterCreateRequestSchemaでバリデーションする
**Then**: "デッキ名は100文字以内で入力してください"エラーが返される

---

### DeckMasterCreateRequestSchema - 無効なclassName 🔵

**信頼性**: 🔵 *クラス名検証テスト*

**Given**: CLASS_NAMESに含まれない"無効クラス"が与えられる
**When**: DeckMasterCreateRequestSchemaでバリデーションする
**Then**: "無効なクラス名です"エラーが返される

---

### DeckMasterUpdateRequestSchema - deckNameのみ 🔵

**信頼性**: 🔵 *更新スキーマテスト*

**Given**: 有効なdeckName（"更新デッキ名"）が与えられる
**When**: DeckMasterUpdateRequestSchemaでバリデーションする
**Then**: バリデーションが成功する

---

### MyDeckCreateRequestSchema - deckCode空白 🔵

**信頼性**: 🔵 *REQ-EXT-107対応*

**Given**: deckCodeが空文字またはundefinedで与えられる
**When**: MyDeckCreateRequestSchemaでバリデーションする
**Then**: バリデーションが成功する（deckCodeは空白可）

---

### MyDeckCreateRequestSchema - 全フィールド有効 🔵

**信頼性**: 🔵 *正常系テスト*

**Given**: 有効なdeckId、deckName、deckCodeが与えられる
**When**: MyDeckCreateRequestSchemaでバリデーションする
**Then**: バリデーションが成功する

---

### zodErrorToValidationError - 変換テスト 🔵

**信頼性**: 🔵 *エラー変換テスト*

**Given**: ZodErrorオブジェクトが与えられる
**When**: zodErrorToValidationError関数を呼び出す
**Then**: ValidationError形式に変換される

---

## 実装手順

### TDDタスクの場合
1. `/tsumiki:tdd-requirements TASK-0003` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

### 詳細手順

1. **テストファイル作成**: `packages/shared/src/validation/__tests__/deck-master.test.ts`
2. **DeckMasterCreateRequestSchemaのテスト作成**: 正常系・異常系
3. **DeckMasterCreateRequestSchemaの実装**: 最小実装でテストをパス
4. **DeckMasterUpdateRequestSchemaのテスト作成**: 正常系
5. **DeckMasterUpdateRequestSchemaの実装**: 最小実装でテストをパス
6. **MyDeckCreateRequestSchemaのテスト作成**: deckCode空白許可含む
7. **MyDeckCreateRequestSchemaの実装**: 最小実装でテストをパス
8. **zodErrorToValidationErrorのテスト作成**: 変換テスト
9. **zodErrorToValidationErrorの実装**: 最小実装でテストをパス
10. **リファクタリング**: 重複排除、コード品質改善
11. **統合テスト**: 全テストがパスすることを確認

---

## 既存コードとの整合性

### 既存deck-master.tsの状況

既存の`deck-master.ts`には以下が定義されている：
- `ClassNameSchema`: z.enumで定義済み（CLASS_NAMES定数への切り替えを検討）
- `NewDeckMasterSchema`: id, className, deckName, sortOrderを含む（拡張または並存）
- `UpdateDeckMasterSchema`: partial形式で定義済み

### 既存my-deck.tsの状況

既存の`my-deck.ts`には以下が定義されている：
- `DeckCodeSchema`: min(1)で必須になっている → **空白可に変更が必要**
- `NewMyDeckSchema`: deckCodeが必須 → **deckId追加、deckCode任意に変更**

### 対応方針

1. 既存スキーマは後方互換性のため残す
2. 新しいスキーマ（DeckMasterCreateRequestSchema等）を追加
3. deckCodeの空白許可は新スキーマで対応

---

## 信頼性レベルサマリー

- **総項目数**: 12項目
- 🔵 **青信号**: 12項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（すべてのバリデーションルールがrequirements.mdに基づく）
