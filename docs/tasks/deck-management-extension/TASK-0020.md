# TASK-0020: MyDeckDialogコンポーネント実装

## タスク概要

- **タスクID**: TASK-0020
- **タイトル**: MyDeckDialogコンポーネント実装
- **タイプ**: TDD
- **推定工数**: 2時間
- **フェーズ**: Phase 3 - 使用デッキ管理機能
- **信頼性**: 🔵（interfaces.ts MyDeckDialogPropsより）
- **依存タスク**: TASK-0019
- **後続タスク**: TASK-0021
- **ステータス**: [ ] 未着手

---

## 要件

### 機能要件

- deckId 選択（DeckMaster の className で表示、id で登録）
- deckName 自由入力
- deckCode 自由入力（任意）
- バリデーションエラー表示

---

## 実装詳細

### 1. コンポーネントProps定義

```typescript
// packages/web/src/components/deck/types.ts

import type { DeckMaster } from '@shadowverse-log/shared';

export interface MyDeckDialogProps {
  /** ダイアログ表示状態 */
  isOpen: boolean;
  /** DeckMaster一覧（選択肢用） */
  deckMasters: DeckMaster[];
  /** ローディング状態 */
  isSubmitting: boolean;
  /** 閉じるコールバック */
  onClose: () => void;
  /** 登録コールバック */
  onSubmit: (input: CreateMyDeckInput) => Promise<void>;
}

export interface CreateMyDeckInput {
  deckId: string;
  deckName: string;
  deckCode?: string;
}
```

### 2. コンポーネント実装

```typescript
// packages/web/src/components/deck/MyDeckDialog.tsx

import { FC, useState, useEffect } from 'react';
import { Button } from '../common/Button';
import { Input } from '../common/Input';
import { Select } from '../common/Select';
import type { MyDeckDialogProps, CreateMyDeckInput } from './types';

interface FormErrors {
  deckId?: string;
  deckName?: string;
}

export const MyDeckDialog: FC<MyDeckDialogProps> = ({
  isOpen,
  deckMasters,
  isSubmitting,
  onClose,
  onSubmit,
}) => {
  const [formData, setFormData] = useState<CreateMyDeckInput>({
    deckId: '',
    deckName: '',
    deckCode: '',
  });
  const [errors, setErrors] = useState<FormErrors>({});

  // ダイアログが開くたびにフォームをリセット
  useEffect(() => {
    if (isOpen) {
      setFormData({ deckId: '', deckName: '', deckCode: '' });
      setErrors({});
    }
  }, [isOpen]);

  const validate = (): boolean => {
    const newErrors: FormErrors = {};

    if (!formData.deckId) {
      newErrors.deckId = 'クラスを選択してください';
    }

    if (!formData.deckName.trim()) {
      newErrors.deckName = 'デッキ名を入力してください';
    }

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();

    if (!validate()) {
      return;
    }

    try {
      await onSubmit({
        deckId: formData.deckId,
        deckName: formData.deckName.trim(),
        deckCode: formData.deckCode?.trim() || undefined,
      });
      onClose();
    } catch (error) {
      // エラーは親コンポーネントでハンドリング
    }
  };

  if (!isOpen) return null;

  // DeckMasterをclassNameでグループ化し、選択肢を作成
  const deckOptions = deckMasters.map((deck) => ({
    value: deck.id,
    label: deck.className,
  }));

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center"
      data-testid="mydeck-dialog"
    >
      {/* オーバーレイ */}
      <div
        className="absolute inset-0 bg-black/50"
        onClick={onClose}
        data-testid="mydeck-dialog-overlay"
      />

      {/* ダイアログ本体 */}
      <div className="relative bg-white rounded-lg shadow-xl p-6 max-w-md w-full mx-4">
        <h2 className="text-lg font-semibold mb-4" data-testid="mydeck-dialog-title">
          使用デッキを追加
        </h2>

        <form onSubmit={handleSubmit} className="space-y-4">
          {/* クラス選択 */}
          <div>
            <label
              htmlFor="deckId"
              className="block text-sm font-medium text-gray-700 mb-1"
            >
              クラス <span className="text-red-500">*</span>
            </label>
            <Select
              id="deckId"
              value={formData.deckId}
              onChange={(value) => setFormData({ ...formData, deckId: value })}
              options={deckOptions}
              placeholder="クラスを選択"
              error={errors.deckId}
              data-testid="deck-class-select"
            />
            {errors.deckId && (
              <p className="mt-1 text-sm text-red-500" data-testid="deck-id-error">
                {errors.deckId}
              </p>
            )}
          </div>

          {/* デッキ名入力 */}
          <div>
            <label
              htmlFor="deckName"
              className="block text-sm font-medium text-gray-700 mb-1"
            >
              デッキ名 <span className="text-red-500">*</span>
            </label>
            <Input
              id="deckName"
              type="text"
              value={formData.deckName}
              onChange={(e) => setFormData({ ...formData, deckName: e.target.value })}
              placeholder="例: 秘術オデンスペル"
              error={errors.deckName}
              data-testid="deck-name-input"
            />
            {errors.deckName && (
              <p className="mt-1 text-sm text-red-500" data-testid="deck-name-error">
                {errors.deckName}
              </p>
            )}
          </div>

          {/* デッキコード入力（任意） */}
          <div>
            <label
              htmlFor="deckCode"
              className="block text-sm font-medium text-gray-700 mb-1"
            >
              デッキコード <span className="text-gray-400">(任意)</span>
            </label>
            <Input
              id="deckCode"
              type="text"
              value={formData.deckCode || ''}
              onChange={(e) => setFormData({ ...formData, deckCode: e.target.value })}
              placeholder="例: ABC123DEF456"
              data-testid="deck-code-input"
            />
          </div>

          {/* ボタン */}
          <div className="flex justify-end gap-3 pt-4">
            <Button
              type="button"
              variant="secondary"
              onClick={onClose}
              disabled={isSubmitting}
              data-testid="cancel-button"
            >
              キャンセル
            </Button>
            <Button
              type="submit"
              variant="primary"
              disabled={isSubmitting}
              data-testid="submit-button"
            >
              {isSubmitting ? '登録中...' : '登録'}
            </Button>
          </div>
        </form>
      </div>
    </div>
  );
};
```

### 3. Storybook Story

```typescript
// packages/web/src/components/deck/MyDeckDialog.stories.tsx

import type { Meta, StoryObj } from '@storybook/react';
import { MyDeckDialog } from './MyDeckDialog';

const meta: Meta<typeof MyDeckDialog> = {
  title: 'Deck/MyDeckDialog',
  component: MyDeckDialog,
  tags: ['autodocs'],
};

export default meta;
type Story = StoryObj<typeof MyDeckDialog>;

const sampleDeckMasters = [
  { id: 'deck-001', className: 'ウィッチ', deckName: 'ウィッチ', sortOrder: 1 },
  { id: 'deck-002', className: 'ロイヤル', deckName: 'ロイヤル', sortOrder: 2 },
  { id: 'deck-003', className: 'ナイトメア', deckName: 'ナイトメア', sortOrder: 3 },
  { id: 'deck-004', className: 'ドラゴン', deckName: 'ドラゴン', sortOrder: 4 },
  { id: 'deck-005', className: 'ビショップ', deckName: 'ビショップ', sortOrder: 5 },
  { id: 'deck-007', className: 'エルフ', deckName: 'エルフ', sortOrder: 7 },
  { id: 'deck-008', className: 'ネメシス', deckName: 'ネメシス', sortOrder: 8 },
];

export const Default: Story = {
  args: {
    isOpen: true,
    deckMasters: sampleDeckMasters,
    isSubmitting: false,
    onClose: () => console.log('Close clicked'),
    onSubmit: async (input) => {
      console.log('Submit:', input);
      await new Promise((resolve) => setTimeout(resolve, 1000));
    },
  },
};

export const Submitting: Story = {
  args: {
    isOpen: true,
    deckMasters: sampleDeckMasters,
    isSubmitting: true,
    onClose: () => {},
    onSubmit: async () => {},
  },
};

export const Closed: Story = {
  args: {
    isOpen: false,
    deckMasters: sampleDeckMasters,
    isSubmitting: false,
    onClose: () => {},
    onSubmit: async () => {},
  },
};
```

---

## テスト要件

### テストケース一覧

| No | テストケース | 期待結果 | 優先度 |
|----|-------------|----------|--------|
| 1 | DeckMasterのclassNameが選択肢に表示されること | 選択肢表示 | 高 |
| 2 | 選択したclassNameに対応するidがdeckIdに設定されること | 正しいid設定 | 高 |
| 3 | deckName空で登録ボタン押下時にエラー表示 | バリデーションエラー | 高 |
| 4 | deckId未選択で登録ボタン押下時にエラー表示 | バリデーションエラー | 高 |
| 5 | 登録成功でダイアログが閉じること | onClose呼び出し | 高 |
| 6 | キャンセルボタンでダイアログが閉じること | onClose呼び出し | 中 |
| 7 | isSubmitting=trueでボタンが無効化されること | disabled状態 | 中 |

### テストコード例

```typescript
// packages/web/src/components/deck/__tests__/MyDeckDialog.test.tsx

import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { MyDeckDialog } from '../MyDeckDialog';

const sampleDeckMasters = [
  { id: 'deck-001', className: 'ウィッチ', deckName: 'ウィッチ', sortOrder: 1 },
  { id: 'deck-002', className: 'ロイヤル', deckName: 'ロイヤル', sortOrder: 2 },
];

describe('MyDeckDialog', () => {
  const defaultProps = {
    isOpen: true,
    deckMasters: sampleDeckMasters,
    isSubmitting: false,
    onClose: vi.fn(),
    onSubmit: vi.fn().mockResolvedValue(undefined),
  };

  beforeEach(() => {
    vi.clearAllMocks();
  });

  describe('表示', () => {
    it('ダイアログが表示されること', () => {
      render(<MyDeckDialog {...defaultProps} />);

      expect(screen.getByTestId('mydeck-dialog')).toBeInTheDocument();
      expect(screen.getByTestId('mydeck-dialog-title')).toHaveTextContent('使用デッキを追加');
    });

    it('isOpen=falseでダイアログが表示されないこと', () => {
      render(<MyDeckDialog {...defaultProps} isOpen={false} />);

      expect(screen.queryByTestId('mydeck-dialog')).not.toBeInTheDocument();
    });

    it('DeckMasterのclassNameが選択肢に表示されること', () => {
      render(<MyDeckDialog {...defaultProps} />);

      const select = screen.getByTestId('deck-class-select');
      expect(select).toBeInTheDocument();
    });
  });

  describe('フォーム入力', () => {
    it('クラス選択でdeckIdが設定されること', async () => {
      const user = userEvent.setup();
      const onSubmit = vi.fn().mockResolvedValue(undefined);
      render(<MyDeckDialog {...defaultProps} onSubmit={onSubmit} />);

      // クラス選択
      const select = screen.getByTestId('deck-class-select');
      await user.click(select);
      await user.click(screen.getByText('ウィッチ'));

      // デッキ名入力
      const nameInput = screen.getByTestId('deck-name-input');
      await user.type(nameInput, 'テストデッキ');

      // 登録
      await user.click(screen.getByTestId('submit-button'));

      await waitFor(() => {
        expect(onSubmit).toHaveBeenCalledWith({
          deckId: 'deck-001',
          deckName: 'テストデッキ',
          deckCode: undefined,
        });
      });
    });
  });

  describe('バリデーション', () => {
    it('deckName空で登録ボタン押下時にエラー表示', async () => {
      const user = userEvent.setup();
      render(<MyDeckDialog {...defaultProps} />);

      // クラスのみ選択
      const select = screen.getByTestId('deck-class-select');
      await user.click(select);
      await user.click(screen.getByText('ウィッチ'));

      // 登録ボタンクリック
      await user.click(screen.getByTestId('submit-button'));

      expect(screen.getByTestId('deck-name-error')).toHaveTextContent('デッキ名を入力してください');
    });

    it('deckId未選択で登録ボタン押下時にエラー表示', async () => {
      const user = userEvent.setup();
      render(<MyDeckDialog {...defaultProps} />);

      // デッキ名のみ入力
      const nameInput = screen.getByTestId('deck-name-input');
      await user.type(nameInput, 'テストデッキ');

      // 登録ボタンクリック
      await user.click(screen.getByTestId('submit-button'));

      expect(screen.getByTestId('deck-id-error')).toHaveTextContent('クラスを選択してください');
    });
  });

  describe('登録処理', () => {
    it('登録成功でダイアログが閉じること', async () => {
      const user = userEvent.setup();
      const onClose = vi.fn();
      const onSubmit = vi.fn().mockResolvedValue(undefined);
      render(<MyDeckDialog {...defaultProps} onClose={onClose} onSubmit={onSubmit} />);

      // フォーム入力
      const select = screen.getByTestId('deck-class-select');
      await user.click(select);
      await user.click(screen.getByText('ウィッチ'));

      const nameInput = screen.getByTestId('deck-name-input');
      await user.type(nameInput, 'テストデッキ');

      // 登録
      await user.click(screen.getByTestId('submit-button'));

      await waitFor(() => {
        expect(onClose).toHaveBeenCalled();
      });
    });

    it('キャンセルボタンでダイアログが閉じること', async () => {
      const user = userEvent.setup();
      const onClose = vi.fn();
      render(<MyDeckDialog {...defaultProps} onClose={onClose} />);

      await user.click(screen.getByTestId('cancel-button'));

      expect(onClose).toHaveBeenCalled();
    });
  });

  describe('ローディング状態', () => {
    it('isSubmitting=trueでボタンが無効化されること', () => {
      render(<MyDeckDialog {...defaultProps} isSubmitting={true} />);

      expect(screen.getByTestId('submit-button')).toBeDisabled();
      expect(screen.getByTestId('cancel-button')).toBeDisabled();
    });

    it('isSubmitting=trueで登録中テキストが表示されること', () => {
      render(<MyDeckDialog {...defaultProps} isSubmitting={true} />);

      expect(screen.getByTestId('submit-button')).toHaveTextContent('登録中...');
    });
  });
});
```

---

## 完了条件

- [ ] MyDeckDialogコンポーネントが実装されている
- [ ] DeckMasterのclassNameで選択肢が表示される
- [ ] 選択したclassNameに対応するidがdeckIdに設定される
- [ ] deckName, deckCodeの入力ができる
- [ ] バリデーションエラーが表示される
- [ ] 登録成功でダイアログが閉じる
- [ ] すべてのテストケースが成功する
- [ ] Storybookでコンポーネントが確認できる

---

## 実行コマンド

```bash
# TDDフロー
/tsumiki:tdd-red           # テスト実装（失敗）
/tsumiki:tdd-green         # 最小実装（成功）
/tsumiki:tdd-refactor      # リファクタリング
/tsumiki:tdd-verify-complete  # 品質確認
```

---

## 検証手順

1. テストが失敗することを確認（Red）
2. 最小限の実装でテストが成功することを確認（Green）
3. リファクタリング後もテストが成功することを確認
4. `pnpm test` で全テストが成功することを確認
5. `pnpm storybook` でコンポーネントが正しく表示されることを確認
6. `pnpm lint` でエラーがないことを確認

---

## 参考資料

- [interfaces.ts - MyDeckDialogProps](../../../docs/design/deck-management-extension/interfaces.ts)
- [React Testing Library](https://testing-library.com/docs/react-testing-library/intro/)
- [Storybook Documentation](https://storybook.js.org/docs)
